# direnv設定ファイル
# direnvがインストールされている場合、このディレクトリに入ると自動で環境変数が設定されます
# インストール: brew install direnv (macOS) または sudo apt install direnv (Ubuntu)
# 設定: echo 'eval "$(direnv hook bash)"' >> ~/.bashrc

# AWS設定
export AWS_PROFILE=shota
export AWS_REGION=ap-northeast-1

# Node.js環境
export NODE_ENV=development

# プロジェクト情報
export PROJECT_NAME=makeoo
export PROJECT_ENV=development

echo "✨ Makeoo環境変数が設定されました (AWS_PROFILE=$AWS_PROFILE)"